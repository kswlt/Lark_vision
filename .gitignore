# 密钥与本地环境
backend/.env
*.env
!backend/.env.example

# 依赖与构建
frontend/node_modules/
backend/.venv/
__pycache__/
*.pyc

# 日志
logs/*.log

# 本地构建产物（希沃运行时需要 dist，但可由 frontend/dist 重新构建）
frontend/dist/
dist/

# 大体积日志（不入库）
logs/capture.txt

# 人脸识别数据（照片/模型/打卡记录，隐私不入库）
face_library/
data/
backend/data/
